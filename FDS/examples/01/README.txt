This example runs first the main_cfd.py script, which computes the CFD task and saves fields of AST into files.
The second script main_tm.py runs the thermal task with AST loaded from given files.
Both scripts can be executed using existing makefile.